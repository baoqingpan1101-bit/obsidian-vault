---
title: Obsidian 插件配置优化方案
created: 2026-03-30
updated: 2026-03-30
tags: [系统, Obsidian, 配置]
---

# Obsidian 插件配置优化方案

## 当前已安装插件（17 个）

| 插件 | 状态 | 建议 |
|------|------|------|
| dataview | ✅ 核心保留 | 数据查询核心 |
| calendar | ✅ 核心保留 | 日记导航 |
| templater-obsidian | ✅ 核心保留 | 模板引擎 |
| obsidian-excalidraw-plugin | ✅ 核心保留 | 工程绘图 |
| copilot | ✅ 核心保留 | AI 对话 |
| nutstore-sync | ✅ 核心保留 | 多设备同步 |
| obsidian-tasks-plugin | ✅ 核心保留 | 任务管理 |
| table-editor-obsidian | ✅ 核心保留 | 表格编辑 |
| obsidian-local-rest-api | ✅ 核心保留 | 外部自动化 |
| quickadd | ✅ 核心保留 | 快速捕获 |
| obsidian-git | ✅ 核心保留 | 版本管理 |
| obsidian-image-auto-upload-plugin | ✅ 保留 | 图片上传 |
| obsidian-emoji-toolbar | ⚪ 可选保留 | 辅助工具 |
| obsidian-mind-map | 🟡 功能重叠 | 与 Excalidraw 重叠，可卸载 |
| flashcards-obsidian | 🟡 按需保留 | 专业术语记忆，未使用可卸载 |
| obsidian-kanban | 🟡 按需保留 | 项目看板，未使用可卸载 |
| obsidian-day-planner | 🟡 功能重叠 | 与 Tasks 重叠，可卸载 |

---

## 一、Templater 配置优化

### 当前配置检查

**✅ 已正确配置：**
- `templates_folder`: `00-系统/templates` ✅
- `trigger_on_file_creation`: `true` ✅

**❌ 需要修复：**
- `folder_templates[0].folder`: `20-日志/日记` → 应改为 `06-日记`

### 修复步骤

设置 → Templater → Folder Templates

将 `20-日志/日记` 修改为 `06-日记`

---

## 二、QuickAdd 配置

### 当前状态

**❌ 未配置任何命令**（`choices: []`）

### 建议配置

#### 命令 1：快速收集到收件箱

设置 → QuickAdd → Manage Macros

```
类型：Capture
名称：快速收集
File Name：01-收件箱/收件箱.md
Capture format：
  - {{DATE:YYYY-MM-DD HH:mm}} {{VALUE}}
Append to bottom：开启
```

#### 命令 2：新建日记

```
类型：Template
名称：新建日记
Template Path：00-系统/templates/daily-note-template.md
File Name Format：{{DATE:YYYY-MM-DD}}
Create in folder：06-日记/{{DATE:YYYY}}/
```

---

## 三、Local REST API 配置

### 当前配置

- **端口**：27124（HTTPS）、27123（HTTP，未启用）
- **API Key**：`38e5f2eead376cf260853325a773b58b21147a64071eae2b5dd24e0320f8a464`
- **状态**：已配置证书，HTTP 未启用

### 建议调整

设置 → Local REST API

- `Enable insecure server` → 开启（本地开发用 HTTP 更方便）
- 端口 27123 可供外部脚本调用

### Python 调用示例

```python
import requests

BASE_URL = "http://localhost:27123"
API_KEY = "38e5f2eead376cf260853325a773b58b21147a64071eae2b5dd24e0320f8a464"

headers = {
    "Authorization": f"Bearer {API_KEY}",
    "Content-Type": "text/markdown"
}

# 读取收件箱
def read_inbox():
    r = requests.get(f"{BASE_URL}/vault/01-收件箱/收件箱.md", headers=headers)
    return r.text

# 追加内容到收件箱
def append_to_inbox(content):
    r = requests.post(
        f"{BASE_URL}/vault/01-收件箱/收件箱.md",
        headers=headers,
        data=content.encode("utf-8")
    )
    return r.status_code
```

---

## 四、Git 配置

### 当前配置

- **自动保存间隔**：10 分钟
- **自动推送间隔**：5 分钟
- **自动拉取间隔**：5 分钟
- **启动时自动拉取**：开启 ✅

### 建议

当前配置合理，保持不变。

### .gitignore 检查

确保 vault 根目录有 `.gitignore`：

```gitignore
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.trash/
.DS_Store
*.tmp
copilot/
```

---

## 五、Dataview 配置

### 当前配置

- **日期格式**：`MMMM dd, yyyy`（英文格式）
- **内联查询前缀**：`=`

### 建议

将日期格式改为中文友好：

设置 → Dataview

- `Default Date Format` → `YYYY年M月D日`
- `Default Date Time Format` → `YYYY年M月D日 HH:mm`

---

## 已完成的配置修复

| 项目 | 状态 |
|------|------|
| Templater folder_templates 路径 | ✅ 已修复 `20-日志/日记` → `06-日记` |
| .gitignore 路径引用 | ✅ 已更新 |
| Local REST API HTTP 服务 | ✅ 已启用（端口 27123）|

---

## 六、插件精简建议

### 可卸载的插件（功能重叠）

| 插件 | 原因 |
|------|------|
| obsidian-mind-map | Excalidraw 可画思维导图 |
| obsidian-day-planner | Tasks + Calendar 已覆盖 |

### 可选保留的插件

| 插件 | 保留条件 |
|------|----------|
| flashcards-obsidian | 如需记忆专业术语 |
| obsidian-kanban | 如需看板管理工程项目 |

---

## 七、完整工作流

```
手机/电脑产生想法
    ↓
QuickAdd 快速捕获 → 01-收件箱/
    ↓
Local REST API 供外部脚本读取
    ↓
AI 脚本处理：补 frontmatter / 生成卡片 / 分类归档
    ↓
Git 自动提交 → 版本快照保存
    ↓
Nutstore 同步 → 多设备可用
```

---

## 附录：API Key 备份

**Local REST API Key**（请妥善保管）：
```
38e5f2eead376cf260853325a773b58b21147a64071eae2b5dd24e0320f8a464
```

---

*此文档由 AI 代理生成，用于记录和优化 Obsidian 插件配置。*
